#!/bin/sh

optimize='OO'; case "$*" in *--debug*) unset optimize; esac
# Launch live-installer with all passed arguments (future reserved)
cd /usr/lib/ ; python2.7 -tt${optimize} -m live-installer.main "$@"
